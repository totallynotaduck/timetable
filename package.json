{
  "name": "timetable-app",
  "version": "1.2.0",
  "private": true,
  "description": "Timetable app with a multi-device account system. Static front-end on GitHub Pages, data synced via Upstash Redis REST (no backend server).",
  "dependencies": {}
}
